language: python
python:
  - 2.6
  - 2.7
  - 2.7.8
env:
  - DJANGO_VERSION=1.9
  - DJANGO_VERSION=1.10
  - DJANGO_VERSION=1.11
install:
  - pip install -r conf/requirements-dev.txt
  - pip install coveralls
script:
  - coverage run --source='.' manage.py test
after_success:
  - coveralls
